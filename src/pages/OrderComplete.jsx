import React from 'react';
import styles from './OrderComplete.module.scss';

const OrderComplete = () => {
  return (
    <div className={styles.OrderComplete}></div>
  );
};

export default OrderComplete;
